---
id: 6f8a
type: tech_setup
tags:
- validation
- examples
status: active
created_at: '2026-04-13T21:16:43Z'
updated_at: '2026-04-13T21:16:43Z'
metadata: {}
custom: {}
---

# Validate the package with dart test and dart analyze; validate the real example by running flutter pub get and dart run slang in each package root, then dart run build_runner build --delete-conflicting-outputs from the root app.

## Context

The repository includes both unit-style coverage for generation behavior and end-to-end example fixtures under `example/`.

## Content

Baseline validation commands from the repo root:

- `dart test`
- `dart analyze`

For `example/integration_check_2`, regenerate dependent slang outputs in each package first, then run build_runner from the root app to validate canonical-plus-copy generation.

## Consequences / notes

When documentation changes the described workflow, the examples should be rerun so the checked-in outputs remain trustworthy.
